version: '3.8'

services:
  auth-service:
    build: ./auth-service
    ports:
      - "3001:3001"
    env_file:
      - ./auth-service/.env

  app-service:
    build: ./app-service
    ports:
      - "3002:3002"
    env_file:
      - ./app-service/.env
